# Установка Nightscout Extended Integration

## Быстрый старт

### Шаг 1: Установка интеграции

#### Вариант A: Ручная установка

1. Скачайте или клонируйте этот репозиторий
2. Скопируйте папку `nightscout` в `config/custom_components/`:
   ```bash
   # На вашем Home Assistant хосте
   cd /config
   mkdir -p custom_components
   cp -r /path/to/HA_Nightscout/nightscout custom_components/
   ```

3. Перезапустите Home Assistant:
   - Настройки → Система → Перезапустить

#### Вариант B: Установка через Git

```bash
cd /config/custom_components
git clone https://github.com/yourusername/HA_Nightscout.git
mv HA_Nightscout/nightscout ./
rm -rf HA_Nightscout
```

### Шаг 2: Добавление интеграции

1. В Home Assistant перейдите: **Настройки** → **Устройства и службы**
2. Нажмите **+ Добавить интеграцию**
3. Найдите "**Nightscout Extended**"
4. Введите данные:
   - **URL**: `https://your-nightscout-url.com`
   - **API Key**: (если требуется)
5. Нажмите **Отправить**

### Шаг 3: Проверка

После успешной настройки вы должны увидеть новое устройство "Nightscout" со всеми сенсорами в **Настройки** → **Устройства и службы** → **Nightscout Extended**.

## Требования к Nightscout серверу

### Минимальные требования

- Nightscout версии 14.0.0 или выше
- Доступ к серверу через HTTPS (рекомендуется)
- API доступ (настройка `AUTH_DEFAULT_ROLES`)

### Настройка Nightscout для полной функциональности

Для получения всех данных (IOB, COB, чувствительность) необходимо:

1. **Включить плагины** в настройках Nightscout (`ENABLE` в переменных окружения):
   ```
   ENABLE=careportal openaps pump iob sage cage
   ```

2. **Настроить права доступа** (если используете API ключ):
   ```
   AUTH_DEFAULT_ROLES=readable
   ```
   
   Или для защищенного доступа:
   ```
   AUTH_DEFAULT_ROLES=denied
   API_SECRET=your_secret_key
   ```

3. **Убедиться, что AndroidAPS отправляет данные**:
   - В AndroidAPS: Конфигурация → NSClient → Включено
   - URL Nightscout указан правильно
   - Логи подтверждают успешную отправку

### Проверка API

Проверьте доступность данных через браузер:

**Проверка devicestatus (IOB, COB, помпа):**
```
https://your-nightscout-url.com/api/v1/devicestatus.json?count=1
```

**Проверка entries (глюкоза):**
```
https://your-nightscout-url.com/api/v1/entries.json?count=1
```

**Проверка статуса сервера:**
```
https://your-nightscout-url.com/api/v1/status.json
```

## Конфигурация через configuration.yaml (не требуется)

Эта интеграция использует ConfigFlow (UI конфигурация), поэтому **не требует** записей в `configuration.yaml`.

Однако, если вы хотите настроить логирование:

```yaml
# configuration.yaml

logger:
  default: warning
  logs:
    custom_components.nightscout: info
    py_nightscout: info
```

Для отладки установите `debug`:

```yaml
logger:
  default: warning
  logs:
    custom_components.nightscout: debug
    py_nightscout: debug
```

## Установка Dashboard

### Автоматическая настройка (копирование YAML)

1. Откройте файл `nightscout_dashboard.yaml` из репозитория
2. Скопируйте весь его содержимое
3. В Home Assistant:
   - Откройте **Обзор**
   - Нажмите **✏️ Редактировать панель управления** (правый верхний угол)
   - Нажмите **+ Добавить карточку**
   - Выберите **Вручную** (Manual)
   - Вставьте скопированный код
   - Нажмите **Сохранить**

### Ручная настройка карточек

Если хотите настроить вручную, вот основные карточки:

#### Gauge глюкозы:
```yaml
type: gauge
entity: sensor.blood_sugar
min: 40
max: 400
severity:
  green: 70
  yellow: 180
  red: 250
```

#### Список сенсоров:
```yaml
type: entities
title: Nightscout
entities:
  - sensor.blood_sugar
  - sensor.nightscout_insulin_on_board
  - sensor.nightscout_carbs_on_board
  - sensor.nightscout_sensitivity_ratio
```

#### График глюкозы:
```yaml
type: history-graph
entities:
  - sensor.blood_sugar
hours_to_show: 6
```

### Установка mini-graph-card (опционально)

Для продвинутых графиков eA1c:

1. Откройте **HACS** → **Frontend**
2. Нажмите **+ Исследовать и скачать репозитории**
3. Найдите "**mini-graph-card**"
4. Нажмите **Скачать**
5. Перезагрузите страницу браузера (Ctrl+F5)

## Настройка уведомлений

### Критически низкая глюкоза

```yaml
# automations.yaml
- id: 'low_glucose_critical'
  alias: 'Критически низкая глюкоза'
  trigger:
    - platform: numeric_state
      entity_id: sensor.blood_sugar
      below: 55
  action:
    - service: notify.mobile_app_your_phone
      data:
        title: '🚨 КРИТИЧЕСКИ НИЗКАЯ ГЛЮКОЗА'
        message: 'Глюкоза: {{ states("sensor.blood_sugar") }} мг/дл'
        data:
          priority: high
          ttl: 0
          channel: alarm_stream
    - service: persistent_notification.create
      data:
        title: 'Критически низкая глюкоза'
        message: 'Немедленно примите углеводы!'
```

### Высокая глюкоза с задержкой

```yaml
- id: 'high_glucose_sustained'
  alias: 'Устойчиво высокая глюкоза'
  trigger:
    - platform: numeric_state
      entity_id: sensor.blood_sugar
      above: 250
      for:
        minutes: 30
  action:
    - service: notify.mobile_app_your_phone
      data:
        title: '⚠️ Высокая глюкоза'
        message: 'Глюкоза выше 250 мг/дл более 30 минут'
```

### Низкий инсулин в помпе

```yaml
- id: 'low_reservoir'
  alias: 'Низкий уровень инсулина в резервуаре'
  trigger:
    - platform: numeric_state
      entity_id: sensor.nightscout_pump_reservoir
      below: 20
  condition:
    - condition: time
      after: '08:00:00'
      before: '22:00:00'
  action:
    - service: notify.mobile_app_your_phone
      data:
        title: '💉 Низкий уровень инсулина'
        message: 'В резервуаре осталось {{ states("sensor.nightscout_pump_reservoir") }} U'
```

## Частые проблемы

### Проблема: "Сенсоры показывают unavailable"

**Решение:**

1. Проверьте логи:
   ```yaml
   # configuration.yaml
   logger:
     logs:
       custom_components.nightscout: debug
   ```

2. Проверьте доступность Nightscout API в браузере

3. Убедитесь, что AndroidAPS отправляет данные

### Проблема: "IOB/COB не отображаются"

**Решение:**

1. Проверьте, что AndroidAPS включен и отправляет данные
2. Проверьте devicestatus API:
   ```
   https://your-site.com/api/v1/devicestatus.json?count=1
   ```
3. Убедитесь, что в ответе есть поля `openaps.iob` и `openaps.suggested.COB`

### Проблема: "eA1c не рассчитывается"

**Решение:**

1. Необходимо минимум несколько записей глюкозы за период
2. Проверьте entries API:
   ```
   https://your-site.com/api/v1/entries.json?count=100
   ```
3. Подождите несколько минут для накопления данных

### Проблема: "Интеграция не появляется в списке"

**Решение:**

1. Проверьте правильность размещения:
   ```
   config/
   └── custom_components/
       └── nightscout/
           ├── __init__.py
           ├── manifest.json
           ├── ...
   ```

2. Перезапустите Home Assistant **полностью** (не просто перезагрузка конфигурации)

3. Очистите кэш браузера (Ctrl+Shift+R)

## Дополнительные настройки

### Изменение интервала обновления

По умолчанию: 1 минута. Для изменения отредактируйте `sensor.py`:

```python
# sensor.py
SCAN_INTERVAL = timedelta(minutes=5)  # Изменить на 5 минут
```

### Настройка единиц измерения глюкозы

По умолчанию используются мг/дл. Для поддержки ммоль/л потребуется модификация кода (запланировано в будущей версии).

## Поддержка

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/yourusername/HA_Nightscout/issues)
2. Создайте новый Issue с:
   - Описанием проблемы
   - Версией Home Assistant
   - Логами (если применимо)
   - Версией Nightscout

## Полезные ссылки

- [Документация Nightscout API](https://nightscout.github.io/)
- [AndroidAPS документация](https://androidaps.readthedocs.io/)
- [Home Assistant интеграции](https://www.home-assistant.io/integrations/)
